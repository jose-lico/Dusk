// If included in project, will always be built, forcing the pre build command to take place.
// The pre build command runs embed_assets.py, which embeds the required assets into the project as a char array
